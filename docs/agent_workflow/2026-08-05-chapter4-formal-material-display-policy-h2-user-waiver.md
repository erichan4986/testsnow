# H2 User-Approved Review Waiver

The user explicitly authorized direct implementation and instructed Codex to
skip the Claude review for this H2 batch.

Scope of waiver:

- skip external read-only design review;
- retain Codex self-review, TDD, runtime scope, test gates, and final
  verification;
- do not broaden the allowed files or change producer, renderer, scoring,
  technical, risk, recommendation, or prompt behavior.
