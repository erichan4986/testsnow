# 开发指令：雪球股票舆情监控系统

## 项目目标
为"先生"（用户）构建一个在Mac本地运行的雪球网股票社区舆情监控工具，每周自动生成6只持仓股票的热门/精选帖子分析报告。

## 监控标的（6只）
| 股票名称 | 代码 | 雪球格式 | 东方财富股吧GID |
|---------|------|---------|----------------|
| 黑芝麻智能 | 02533 | HK02533 | 9002533 |
| 长春高新 | 000661 | SZ000661 | 000661 |
| 三花智控 | 002050 | SZ002050 | 002050 |
| 中简科技 | 300777 | SZ300777 | 300777 |
| 圣邦股份 | 300661 | SZ300661 | 300661 |
| 乐鑫科技 | 688188 | SH688188 | 688188 |

## 核心功能需求

### 1. 数据采集
- **来源优先级**：
  - P0: 雪球网（xueqiu.com）——用户明确要求雪球
  - P1: 东方财富股吧（guba.eastmoney.com）——已验证可抓取的备用源
- **采集内容**：
  - 每只股票的热门帖子列表（按热度/阅读量排序）
  - 精选帖子（雪球"精华"标识的帖子）
  - 帖子评论区（用户认可评论区价值，不满足于表面信息）
- **数量**：每只股票采集TOP 10-15条帖子

### 2. 数据筛选标准
- **热门帖**：高阅读量 + 高评论数
- **精品帖**：内容质量高、观点鲜明、数据支撑充分的帖子（需AI判断）
- **评论区**：高赞评论、有实质观点的评论

### 3. AI分析增强
- **情感分析**：判断帖子/评论看多/看空/中性
- **市场情绪总览**：汇总每只股票的整体情绪
- **热门话题提取**：识别讨论焦点（如半导体、AI、医药、新能源等）
- **精品内容深度拆解**：对高质量帖子进行摘要和要点提取

### 4. 报告输出
- **格式**：Markdown报告
- **内容结构**：
  - 市场情绪总览（看多/看空/中性统计）
  - 各股票详情（TOP帖子列表+情感标注+链接）
  - 精品帖子深度分析
  - 热门话题关键词
  - 评论区亮点摘录
- **保存位置**：`~/.openclaw/workspace/reports/xueqiu_report_YYYYMMDD.md`

### 5. 定时执行
- **频率**：每周日晚运行（生成一周报告）
- **方式**：cron定时任务或手动触发

## 技术约束（硬性要求）

### 1. 平台要求
- **运行环境**：Mac电脑本地运行
- **语言**：Python 3.x
- **依赖**：尽量轻量，避免重型框架

### 2. 反检测/账号安全（最高优先级）
- **必须登录状态运行**：使用用户已验证的cookie，不要未登录方案
- **降低检测风险**：
  - 模拟真实浏览器行为（User-Agent用Mac Chrome）
  - 控制请求频率（单股票间隔2-5秒）
  - 避免过于规律的请求模式
  - 使用已有cookie而非频繁重新登录
- **IP问题**：用户cookie在服务器上因IP绑定失效，但Mac本机运行应无此问题

### 3. 已知凭证信息
- 用户已提供过WAF验证后的完整cookie字符串
- `xq_a_token` 可能在localStorage而非cookie中（之前尝试获取为null）
- 如雪球实在无法突破，回退到东方财富股吧方案

### 4. 请求策略
- 优先使用curl（在服务器测试中发现curl比Python requests快且稳定）
- 如需Python requests，设置合理timeout
- HTML解析用正则提取（东方财富方案已验证有效）

## 数据源技术细节

### 雪球网方案（优先尝试）
- **帖子列表API**：`https://xueqiu.com/query/v1/search.json` 或股票讨论区页面
- **精华帖**：`https://xueqiu.com/S/{stock_code}/hot` 或类似接口
- **评论区**：帖子详情页评论接口
- **认证**：cookie中需 `xq_a_token`, `xq_r_token`, `u`, `s` 等字段
- **注意**：雪球有WAF防护（滑块验证码+设备指纹），首次需在Mac浏览器过验证后取cookie

### 东方财富股吧方案（备用/已实现）
- **帖子列表**：`https://guba.eastmoney.com/list,{gid},99_1.html`
- **HTML结构**：`tr.listitem` 包含标题、作者、阅读量、评论数、时间
- **URL格式**：`/news/xxx.html` 或 `//caifuhao.eastmoney.com/news/xxx`
- **优点**：无WAF，curl可直接抓取
- **缺点**：非雪球数据，用户偏好雪球

## 代码结构建议

```
scripts/
  xueqiu_monitor_v2.py      # 主程序
  utils/
    fetcher.py              # 请求封装（curl优先+requests备用）
    parser.py               # HTML/JSON解析
    analyzer.py             # AI情感分析+关键词提取
    reporter.py             # Markdown报告生成
config/
  cookies.txt               # 用户提供的cookie（gitignore）
reports/
  xueqiu_report_YYYYMMDD.md # 输出报告
```

## 实现要点

1. **cookie管理**：从环境变量或配置文件读取cookie，避免硬编码
2. **错误处理**：单股票失败不中断整体流程
3. **日志输出**：打印每只股票采集进度，方便调试
4. **情感分析**：可用简单关键词匹配（已验证有效），或用LLM API（如用户有配置）
5. **报告美化**：用emoji标注情绪，用表格展示数据，增加可读性

## 交付标准
- [ ] Mac本地可直接运行的Python脚本
- [ ] 包含requirements.txt
- [ ] README说明如何配置cookie和运行
- [ ] 首次运行测试通过（至少采集到1只股票数据）
- [ ] 报告格式符合示例结构

## 已知风险与应对

| 风险 | 应对方案 |
|------|---------|
| 雪球WAF拦截 | 使用Mac本机已验证cookie；如失败则降级东方财富 |
| 请求频率过高被封 | 单股票间隔3-5秒；随机抖动 |
| cookie过期 | 提示用户重新在浏览器登录并导出cookie |
| 东方财富结构变更 | 保留HTML解析的正则模式，便于快速修复 |

## 用户偏好补充
- 用户对账号安全高度敏感，任何可能触发风控的操作都要避免
- 偏好轻量交付，不要复杂部署
- 信息筛选标准明确：区分"热门"与"精品"，后者值得深度拆解
- 投资偏好：硬科技（半导体、新材料、机器人零部件）+ 生物医药，细分龙头

---

**指令总结**：在Mac本地用Python构建一个雪球网股票舆情监控工具，使用用户提供的已验证cookie登录后采集6只股票的TOP热门帖子和精华帖，进行AI情感分析，生成Markdown周报。如雪球实在无法突破，回退到已验证的东方财富股吧方案。安全第一，避免触发平台风控。
